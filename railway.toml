[build]
builder = "NIXPACKS"
buildCommand = "npm install --production && cd frontend && npm install && npm run build"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
